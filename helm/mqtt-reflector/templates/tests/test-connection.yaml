apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mqtt-reflector.fullname" . }}-test-connection"
  labels:
    {{- include "mqtt-reflector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mqtt-reflector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
